function Home() {
  return(
    <Card
      bgcolor="secondary"
      txtcolor="white"
      headerText="Home"
      body={`Welcome to Bad Bank. This bank is named "Bad" because it has no security.`}
    />
  );
}