function Home() {
  return(
    <Card
      bgcolor="secondary"
      txtcolor="white"
      headerText="Home Page"
      body={`Welcome to the Bank App. This application features a back end Express server and MongoDB database.`}
    />
  );
}